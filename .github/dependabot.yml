version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(deps)"
    # Keep PR titles conventional so commitlint.yml passes without rewrites.
    groups:
      actions:
        patterns:
          - "*"
